---
title: "Push260906 发布"
lang: zh-CN
---

# Push260906 — 资产更新

[GitHub Release（英文版）](https://github.com/Hope2333/opencode-termux/releases/tag/Push260906)

## 概览

滚动 Push tag 本轮补齐三大家族与 UPX 原始资产，覆盖 v1.18.{15-29} 共 15 个版本，**109 个资产**。

| Family | 包名 | 格式 | 说明 |
|--------|------|------|------|
| native（主线） | `opencode` | deb + pacman `.pkg.tar.xz` | 零 glibc bionic ELF，直跑 |
| glibc（附录） | `opencode-glibc` | deb + pacman `.pkg.tar.xz` | glibc wrapper |
| compressed（压制） | `opencode-compressed` | deb + pacman `.pkg.tar.gz` | UPX --best 压制 |
| upx 资产 | `opencode-native-<ver>-upx.xz` | xz | 压制输入（人类侧产出） |

另随架 `SHA256SUMS.txt`（全量校验）与 `Packages.gz`（apt flat 源索引）。

**互斥关系**：`opencode` ↔ `opencode-glibc` 互斥；`opencode-compressed` Provides `opencode`，三者装其一。

## Install

### 软件源配置（一次性）

```bash
# 一键：配置源并安装（pacman + apt 源一并就位）
curl -fsSL https://hope2333.github.io/repo/install.sh | sh -s -- --install opencode

# 或手动：
# pacman — $PREFIX/etc/pacman.conf 追加：
#   [hope2333-meta]
#   Server = https://hope2333.github.io/repo/Termux/pacman/
#   SigLevel = Optional TrustAll
#   （随后安装 hope2333-mirrorlist，获得 [hope2333] 多 Server 回退源）
# apt — $PREFIX/etc/apt/sources.list.d/hope2333.list 写入：
#   deb [trusted=yes arch=aarch64] https://github.com/Hope2333/opencode-termux/releases/latest/download/ ./
```

### 软件源安装（apt / pacman 均可，均为各包最新版）

```bash
pacman -S opencode              # native 主线
pacman -S opencode-glibc        # glibc 附录线
pacman -S opencode-compressed   # UPX 压制版

apt update && apt install opencode-compressed   # 或 opencode / opencode-glibc
```

> apt flat 索引（`Packages.gz`）只收录每个包名的**最新版**；
> 指定历史版本请用下方手动安装，从 release 资产直取。

### 手动安装（指定版本）

```bash
pacman -U opencode-<ver>-1-aarch64.pkg.tar.xz             # native / glibc
dpkg    -i opencode_<ver>_aarch64.deb
pacman -U opencode-compressed-<ver>-1-aarch64.pkg.tar.gz  # compressed（注意 .gz）
dpkg    -i opencode-compressed_<ver>_aarch64.deb
```

## BUILD LOG

20260904: v1.18.{15-27} native deb 首传
20260905: v1.18.{15-27} native pkg / glibc deb+pkg / compressed deb+pkg / upx.xz 首传
20260906: v1.18.29 native / glibc / compressed 全家族 + upx.xz + Packages.gz + standalone + db 首传
20260907: v1.18.{28-29} native / glibc + upx.xz 补传
20260908: v1.18.28 compressed 补传 + SHA256SUMS.txt
最新版: v1.18.29

## Verification

- 版本连续性：v1.18.{15-29} 无缺口（fleet-push compressed-list 对账 15/15 ✅）
- `SHA256SUMS.txt` 与架上一致
- 家族互斥矩阵声明齐全（conflicts / provides）

## Links

- GitHub: <https://github.com/Hope2333/opencode-termux>
- 软件源指引: <https://hope2333.github.io/guides/software-source.html>
- 文档: `docs/dual-track-install.md` / `docs/transplant.md`
