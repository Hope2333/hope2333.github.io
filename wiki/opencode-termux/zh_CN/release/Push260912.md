---
title: "Push260912 发布"
lang: zh-CN
---

# Push260912 — opencode-wrapper 正式发布

[GitHub Release（英文版）](https://github.com/Hope2333/opencode-termux/releases/tag/Push260912)

## 概览

滚动 Push tag 本轮完成 **包名更名 `opencode-glibc` → `opencode-wrapper`**，并补齐四大家族矩阵，版本 **v1.18.30**。

| Family | 包名 | 格式 | 说明 |
|--------|------|------|------|
| native（主线） | `opencode` | deb + pacman `.pkg.tar.xz` | 零 glibc bionic ELF，直跑 |
| wrapper（附录） | `opencode-wrapper` | deb + pacman `.pkg.tar.xz` | glibc wrapper — 由 `opencode-glibc` 更名而来 |
| compressed（压制） | `opencode-compressed` | deb + pacman `.pkg.tar.gz` | UPX --best 压制 |
| standalone（回滚） | `opencode-wrapper-standalone` | deb + pacman `.pkg.tar.xz` | 冻结单版本回滚包，与 `opencode` 共存 |

另随架 `SHA256SUMS.txt`（全量校验）与 `Packages.gz`（apt flat 源索引）。

## 包名更名：opencode-glibc → opencode-wrapper

glibc 附录家族更名为 `opencode-wrapper`：

- **deb**：`Replaces: opencode-glibc` + `Breaks: opencode-glibc (<< 版本)` —— 既有
  `opencode-glibc` 用户**自动升级**到 `opencode-wrapper`，无需手动操作。
- **pacman**：`replaces=('opencode-glibc')` —— 同样的自动升级语义。

**互斥关系**：`opencode` ↔ `opencode-wrapper` 互斥；`opencode-compressed` Provides
`opencode`，三者装其一。`opencode-wrapper-standalone` 与 `opencode-wrapper` 互斥，
但与 `opencode` 共存（仅回滚路径）。

## Install

### 软件源配置（一次性）

```bash
# 一键：配置源并安装（pacman + apt 源一并就位）
curl -fsSL https://hope2333.github.io/repo/install.sh | sh -s -- --install opencode

# 或手动：
# pacman — $PREFIX/etc/pacman.conf 追加统一 [hope2333] 节（完整写法见
#   /repo/Termux/ 手动配置块），随后 pacman -Sy && pacman -S hope2333-mirrorlist
#   （包安装后钩子把该节收敛为 节头 + Include 托管形态）
# apt — $PREFIX/etc/apt/sources.list.d/hope2333.list 写入：
#   deb [trusted=yes arch=aarch64] https://github.com/Hope2333/opencode-termux/releases/latest/download/ ./
```

### 软件源安装（apt / pacman 均可，均为各包最新版）

```bash
pacman -S opencode              # native 主线
pacman -S opencode-wrapper      # wrapper 附录线（由 opencode-glibc 更名）
pacman -S opencode-compressed   # UPX 压制版

apt update && apt install opencode-compressed   # 或 opencode / opencode-wrapper
```

> apt flat 索引（`Packages.gz`）只收录每个包名的**最新版**；
> 指定历史版本请用下方手动安装，从 release 资产直取。

### 手动安装（指定版本）

```bash
pacman -U opencode-<ver>-1-aarch64.pkg.tar.xz             # native / wrapper
dpkg    -i opencode_<ver>_aarch64.deb
pacman -U opencode-compressed-<ver>-1-aarch64.pkg.tar.gz  # compressed（注意 .gz）
dpkg    -i opencode-compressed_<ver>_aarch64.deb
pacman -U opencode-wrapper-standalone-<ver>-1-aarch64.pkg.tar.xz  # standalone 回滚
dpkg    -i opencode-wrapper-standalone_<ver>_aarch64.deb
```

## BUILD LOG

20260911: wrapper 更名提交 — `e46f5e6` rename -glibc to -wrapper、`7761c6d` add opencode-glibc to Replaces/Breaks for upgrade path、`bff4e76` wrapper replaces only glibc, not native
20260912: `e00298e` glibc→wrapper rename (scripts + docs) + Push260912 发布
最新版: v1.18.30

## Verification

- 包名更名：`opencode-glibc` → `opencode-wrapper`，Replaces/Breaks 升级路径就位
- 版本：v1.18.30 覆盖四大家族
- `SHA256SUMS.txt` 与架上一致
- 家族互斥矩阵声明齐全（conflicts / provides）

## Links

- GitHub: <https://github.com/Hope2333/opencode-termux>
- 软件源指引: <https://hope2333.github.io/guides/software-source.html>
- 文档: `docs/dual-track-install.md` / `docs/transplant.md`